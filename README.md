<!-- Header -->
<div align="center">

![header](https://capsule-render.vercel.app/api?type=rounded&height=170&text=Dialogue%20Summarization&desc=%EC%9D%BC%EC%83%81%20%EB%8C%80%ED%99%94%20%EC%9A%94%EC%95%BD%20NLP%20%EB%8C%80%ED%9A%8C%20%ED%8C%80%20%ED%94%84%EB%A1%9C%EC%A0%9D%ED%8A%B8&fontSize=35&descSize=15&descAlignY=65&color=gradient&fontColor=ffffff&animation=fadeIn)

<h3>🗣️ Encoder–Decoder + LLM 기반 일상 대화 요약 모델 · Dialogue Summarization Competition 🗣️</h3>

</div>

---

## 💻 프로젝트 소개

### 📌 프로젝트 개요

이 리포지토리는 **일상 대화(dialogue)를 입력으로 받아 1~2문장 요약(summary)를 생성하는 모델**을 구축한  
NLP 대회(team)용 코드 및 실험 결과를 정리한 저장소입니다.

- **Task**: Dialogue Summarization (Abstractive Summarization)  
- **Main Models**:  
  - Encoder–Decoder: `digit82/kobart-summarization`, `gogamza/kobart-base-v1`, `wisenut-nlp-team/KoT5-base`  
  - Decoder-only & LLM: `Llama-3.2-1B-Instruct`, `Llama-3-8B-Instruct`, 기타 gemma / HyperCLOVAX 등  
- **Goal**:  
  - 대화 내용을 **사실 기반으로 1~2문장으로 요약**  
  - 다양한 모델/전처리/튜닝 전략 비교  
- **Metric**: ROUGE-1 / ROUGE-2 / ROUGE-L (F1 평균)

> 이 리포지토리는  
> **EDA → 전처리 전략 → KoBART/KoT5 파인튜닝 → LLM 비교 실험 → Ablation & 실패 실험 분석 → 최종 제출**  
> 전체 실험 과정을 팀 단위로 재현할 수 있도록 구성되어 있습니다.

---

## 📂 프로젝트 구조

> 실제 폴더 구성에 맞게 일부 경로를 수정해 사용하시면 됩니다.

```bash
nlp-competition/
│
├── configs/
│   └── config_kobart.yaml           # KoBART 실험 설정
│
├── data/
│   ├── raw/                         # 대회 제공 원본 데이터
│   │   ├── train.csv
│   │   ├── dev.csv
│   │   ├── test.csv
│   │   └── sample_submission.csv
│   └── processed/                   # 전처리 후 데이터
│
├── notebooks/
│   ├── 01_eda/
│   │   ├── eda_overview.ipynb       # 공통 EDA
│   │   └── eda_v2_input_only.ipynb  # 전처리 v2_input_only 분석
│   ├── 02_preprocessing/
│   │   └── preprocessing_v2_input_only.ipynb
│   ├── 03_modeling_kobart/
│   │   ├── modeling_kobart_baseline.ipynb
│   │   └── modeling_kobart_ls_wd.ipynb
│   ├── 04_modeling_kot5/
│   │   └── modeling_kot5.ipynb
│   ├── 05_modeling_llm/
│   │   ├── modeling_llama_1b.ipynb
│   │   └── modeling_llama_8b_qLoRA.ipynb
│   ├── baseline.ipynb               # 대회 제공 baseline 참고
│   └── baseline_solar.ipynb         # Solar API 재요약 실험
│
├── outputs/
│   ├── prediction/
│   │   ├── kobart_v2_input_only.csv
│   │   ├── kobart_v2_input_only_wd0.01_ls0.1.csv
│   │   └── kot5_overlap_inference.csv
│   └── exp_log.csv                  # 실험 로그 (버전/스코어 정리)
│
├── src/
│   ├── data/
│   │   └── preprocessing.py         # 전처리 함수 모듈
│   ├── models/
│   │   ├── train_kobart.py
│   │   ├── train_kot5.py
│   │   └── train_llama_lora.py
│   └── utils/
│       └── metrics.py               # ROUGE 계산 등
│
├── .gitignore
└── README.md
````

---

## 🧪 대회 & 데이터 요약

### 🎯 대회 정보

* **대회 이름**: Dialogue Summarization
* **목표**: 일상 대화를 입력받아 핵심 요약문 1~2문장을 생성
* **평가 지표**: ROUGE-1 / ROUGE-2 / ROUGE-L F1 평균 (reference 3개 평균)

### 🧾 데이터 구성

| 파일        | 개수     | 설명                         |
| --------- | ------ | -------------------------- |
| train.csv | 12,4xx | dialogue + summary + topic |
| dev.csv   | 498    | 검증용                        |
| test.csv  | 499    | summary 없음                 |

각 샘플 예시:

```csv
fname,dialogue,summary,topic
train_0,"#Person1#: ...", "건강검진을 받으러 온 #Person1#이 ...", "건강검진"
```

---

## 🧷 문제 정의 & 요약 품질 기준

모델이 만들어야 할 **“좋은 요약”** 은 다음 조건을 만족해야 합니다.

* 🔄 **역할 보존**

  * `#Person1#`, `#Person2#` 등 화자 역할이 뒤바뀌지 않을 것
* 🧾 **사실 기반**

  * 대화에 없는 내용을 **추가로 지어내지 않을 것 (no hallucination)**
* ✂ **길이 제약**

  * 1~2문장, 원문 길이의 약 20% 이내
* ⏱ **시점 유지**

  * 이미 일어난 일 vs 앞으로 할 일을 구분해서 표현
* ♻ **중복 최소화**

  * 같은 정보를 불필요하게 반복하지 않기

실험 중 자주 관측된 실패 패턴:

* `#Person1#` ↔ `#Person2#` 역할 뒤바뀜
* 존재하지 않는 사건 생성
* 긴 대화에서 사건 순서 꼬임
* 잘못된 CoT(prompt) 붙이기 → 요약 전체 붕괴

> 이 기준을 바탕으로 **입력 템플릿, 전처리 설계, 모델 선택, 후처리 규칙**을 정했습니다.

---

## 🔍 EDA & 전처리 전략

### 1️⃣ EDA 핵심 인사이트

* Dialogue 길이: 평균 **320–360 tokens**, long-tail 분포
* Summary:

  * 대부분 **한 문장**
  * 100~110자 내외, “~다.” 종결
* Topic:

  * summary에 **문자 그대로** 등장하는 비율은 낮지만
  * 의미적인 방향성은 제공
* Noise:

  * “ㅋㅋㅋ, ㅎㅎ, ㅠㅠ, 음…, 아…” 등 감정/감탄사 다수
  * 요약에는 거의 반영되지 않음 → encoder 입장에서 **잡음**

📌 **결론**

* ✂ **입력 전처리는 공격적으로**,
* 🧼 **출력(summary) 전처리는 최소화** 해야 성능이 오른다.

---

### 2️⃣ 전처리 버전 실험 요약

| 버전                | 설명                             | 결과 / 결론                            |
| ----------------- | ------------------------------ | ---------------------------------- |
| v1                | baseline 전처리                   | 참고용                                |
| v2_full           | 입력 + 출력 모두 전처리                 | 출력 손상 → label bias → 개선 없음         |
| **v2_input_only** | **입력만 공격적으로 정제 (noise 제거)**    | **LB +0.5pt 향상 → 최종 채택**           |
| v3                | truncation                     | 중요한 문맥까지 잘려나가 성능 하락                |
| v4                | topic prefix 추가                | 정보 중복/노이즈 증가 → 효과 없음               |
| v5–v6             | 길이 기반 filtering, unigram 필터링 등 | 데이터 분포 왜곡 / abstract sample 제거 → ↓ |

📌 **최종 결론**

> "입력만 전처리하고, 출력(summary)은 절대 손대지 않는다."

---

## 🤖 모델링 개요

이 프로젝트에서는 **Encoder–Decoder 계열(주력)** 과
**Decoder-only LLM 계열(비교/연구용)** 을 함께 실험했습니다.

---

### 1️⃣ Encoder–Decoder 계열 (Main)

#### 🧩 KoBART (`digit82/kobart-summarization`)

**역할**

* 팀 공통 **Baseline & 최종 제출용 주력 모델**

**설정 (최종 세팅)**

* encoder_max_len: 512
* decoder_max_len: 100
* LR: `1e-5`
* Epochs: `20`
* Train batch: 50
* Weight decay: `0.01`
* Label smoothing: `0.1`
* Warmup: 0.1 (cosine scheduler)
* Early stopping: patience=3
* 전처리: **v2_input_only**

#### 🧩 gogamza/kobart-base-v1

**특징**

* 한국어 문장 구조·대화 패턴에 강함
* encoder_max_len을 **1024**까지 확장해 긴 대화 처리

**관찰**

* Public LB: total ROUGE ≈ 50.01
* Private LB: 46.5로 급락 → Public overfit 경향

#### 🧩 KoT5 (`wisenut-nlp-team/KoT5-base`)

**설정**

* encoder_max_len = 896
* decoder_max_len = 80
* LR = `3e-4`
* batch_size = 4 (VRAM 여유 시 8 권장)
* gradient_accumulation_steps = 2
* epochs = 5, fp32
* overlap 기법으로 문장 잘림 일부 완화

**관찰**

* Public: 49.7 (초반 2등 수준)
* Private: **47.5** (일반화 성능 가장 안정적인 축 중 하나)
* 하지만 **시간/효율 면에서 KoBART가 더 유리**하여
  최종 파이프라인은 KoBART 중심으로 구성

---

### 2️⃣ Decoder-only & LLM 계열

#### 🐑 Llama-3.2-1B-Instruct + LoRA

* 학습 시간: 약 46분, 추론 8분
* LoRA 기반 파라미터 효율 튜닝
* 결과: **LB 47.8310**

> ✅ 8B 모델 대비 성능차 거의 없음
> ✅ 학습/추론 속도·비용이 매우 저렴 → 실험 효율 최고

#### 🐑 Llama-3-8B-Instruct + QLoRA

* 3090 환경에서 QLoRA로 학습
* 학습 시간: 약 7시간
* SYSTEM/USER 프롬프트에 **매우 민감**, 잘못 설계 시

  * 설명형 장문 출력
  * 요약 길이 과도 → ROUGE 급락

#### 기타 Decoder-only 모델

* `gemma-7b`, `HyperCLOVAX-SEED-Text-Instruct-1.5B`, `kanana-nano-2.1b` 등
* 일부는 프롬프트/코딩 이슈로 출력 품질이 불안정하여
  **스모크런 수준에서 종료**

---

### 3️⃣ Solar API (후처리/재작성 실험)

* Input: 원문 대화 + KoBART/KoT5 요약
* Task: Solar Pro로 “다시 요약” 시키기 (post-editing)

**결과**

* 사람이 보기에는 훨씬 자연스럽고 부드러운 요약
* 하지만 **ROUGE 기준으로는 KoBART 단독보다 항상 낮음**

> ➡ 실제 서비스라면 “인간 평가 + 자동지표”를 함께 봐야겠지만,
> 이번 대회 세팅(ROUGE 최적화)에서는 **Solar 후처리를 제외**했습니다.

---

## 📊 성능 결과 요약

### ✅ KoBART

| 실험명                              | 전처리      | 주요 설정                | Dev ROUGE-L | LB          |
| -------------------------------- | -------- | -------------------- | ----------- | ----------- |
| baseline_v1                      | baseline | 기본                   | ~0.27       | 47.06       |
| v2_input_only                    | 입력만 정제   | 기본                   | 0.282       | 47.6004     |
| **v2_input_only + WD + LS (최종)** | 입력만 정제   | **LS 0.1 + WD 0.01** | **0.298**   | **47.8581** |

### ✅ KoT5 / 기타

* KoT5-base:

  * Public: 49.7
  * Private: **47.5**

### ✅ LLM

* Llama-3.2-1B + LoRA:

  * LB: **47.8310**
* Llama-3-8B + QLoRA:

  * 1B 대비 뚜렷한 우위 X, 비용은 훨씬 큼

---

## 🧭 전체 파이프라인

```text
               ┌─────────────┐
               │   Raw Data   │
               └──────┬──────┘
                      ▼
          ┌────────────────────────┐
          │        EDA 분석        │
          └─────────┬──────────────┘
                    ▼
      ┌──────────────────────────────┐
      │  Preprocess v2_input_only    │
      │  - noise 제거                │
      │  - leakage 토큰 제거 X       │
      └──────────┬───────────────────┘
                 ▼
     ┌────────────────────────────┐
     │   Tokenization (512/100)   │
     └──────────┬─────────────────┘
                ▼
     ┌────────────────────────────┐
     │  KoBART Fine-tuning (20ep) │
     │  + LS 0.1 + WD 0.01        │
     └──────────┬─────────────────┘
                ▼
  ┌────────────────────────────────────────┐
  │  Best Checkpoint Selection (ROUGE-L)   │
  └───────────────────┬────────────────────┘
                      ▼
       ┌───────────────────────────────┐
       │    Beam Search (num_beams=4)  │
       │  no_repeat_ngram=2, max_len=100 │
       └────────────────┬──────────────┘
                        ▼
              ┌─────────────────┐
              │   Final Output  │
              └─────────────────┘
```

Decoder-only / LLM 실험은 위 파이프라인 중
**“모델링 블록(Modeling)”** 을 교체하여 실험한 별도 라인입니다.

---

## 🛠 사용 방법 (How to Run)

### 1️⃣ 데이터 준비

```bash
data/raw/train.csv
data/raw/dev.csv
data/raw/test.csv
data/raw/sample_submission.csv
```

### 2️⃣ EDA 실행

```bash
notebooks/01_eda/eda_overview.ipynb
notebooks/01_eda/eda_v2_input_only.ipynb
```

### 3️⃣ 전처리 실행

```bash
notebooks/02_preprocessing/preprocessing_v2_input_only.ipynb
```

### 4️⃣ KoBART 모델 학습

```bash
notebooks/03_modeling_kobart/modeling_kobart_ls_wd.ipynb
# 또는
python src/models/train_kobart.py --config configs/config_kobart.yaml
```

### 5️⃣ 제출 파일 생성

```bash
outputs/prediction/kobart_v2_input_only_wd0.01_ls0.1.csv
```

---

## 🙋‍♀️ 팀 & 브랜치 전략

| 브랜치       | 역할                       |
| --------- | ------------------------ |
| **main**  | 최종 리더보드 제출용 / 안정 버전      |
| **dev**   | 공통 코드·전처리·파이프라인 개발       |
| **\*-dev** | 팀원별 실험 브랜치 (KoT5, LLM 등) |

---

## 🧠 회고 & 배운 점

### ✨ 핵심 인사이트

* **입력 전처리만 잘해도** 요약 모델 품질이 크게 향상된다.
* Label smoothing, weight decay 같은 **loss/regularization 설계**가
  decoding 튜닝보다 훨씬 큰 영향을 준다.
* 최신 LLM(Decoder-only)이 항상 최선은 아니며,
  **태스크 특성상 Encoder–Decoder(KoBART, KoT5)가 더 적합**할 수 있다.
* ROUGE 기반 대회에서는
  “자연스러운 문장”보다 **reference와의 겹침**이 더 중요하다.

### 👥 협업 측면에서 얻은 점

* LLM 경진대회는
  한 사람이 모든 실험을 끝까지 파보는 것보다
  **모델·영역별 역할 분담 + 병렬 실험 + 빠른 공유**가 훨씬 중요했다.
* 이미지 분류 대회와 달리,
  **“팀 단위로 움직이는 것이 전제된 태스크”** 라는 것을 실제로 체감했다.

---

## 📫 Contact

버그 제보, 개선 제안, 질문 등은
👉 GitHub Issue 또는 Pull Request로 남겨주세요.
